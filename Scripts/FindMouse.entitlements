<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  只有一個鍵，而且刻意如此。

  每加一個 entitlement 都要說得出「哪一個實測失敗需要它」——沒有那個實測，
  加進來的就是一個沒人知道能不能拿掉的權限，而它會一直留著。

  不需要的（2026-08-17 的六個沙盒前提逐條量過）：

  - 讀被雙擊／拖進來的檔案 → LaunchServices 與拖放各自發 sandbox extension，
    不需要 files.user-selected（見 spec 前提 3）
  - control socket → 綁在自己的容器裡（前提 2、5）
  - 開機啟動 → SMAppService 在沙盒下正常，連自己的 bundle 都讀得到（前提 6）
  - Carbon 熱鍵、NSEvent.mouseLocation、overlay → 2026-08-10 驗過，
    spec 第 13 節的「零系統權限」在沙盒下依然成立

  需要的，只有一個：

  - files.user-selected.read-only → **NSOpenPanel 是第三個機制**，powerbox 不吃
    上面那兩張 extension。少了它，面板**不是報錯而是根本不出現**：
    `runModal()` 當場回 .cancel、`url` 是 nil，與使用者按取消一個字都不差
    （2026-08-17 實測，log 裡看得到 openAndSavePanelService 起來 4ms 後就
    `xpc_connection_cancel()`）。搬移舊 pack 那條路（spec D-2）就是靠它，
    而它由 `theOpenPanelEntitlementIsDeclared` 釘住。
    read-only 而不是 read-write：我們只**讀**使用者選的資料夾，寫入一律
    寫進自己的容器。
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-only</key>
    <true/>
</dict>
</plist>
