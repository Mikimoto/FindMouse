.superpowers/
# agent 的暫存與隔離 worktree。列進來不只是為了整潔：曾經有一個
# 收工契約寫「工作樹要乾淨」的 agent 看到 `?? .claude/` 就 rm -rf 掉，
# 連同另一個 agent 正在用的 worktree 一起清空。
.claude/
# git-flow 的 feature worktree。理由同上：它出現在 git status 的 untracked 清單裡，
# 而「把工作樹弄乾淨」很容易被理解成可以刪掉它——那會連整個分支的工作目錄一起沒。
.worktree/
.DS_Store
.build/
*.xcuserstate
# 前導斜線的理由同下面素材那三行：沒有它，任何深度的 build/ 都會被吃掉
# （實測 Sources/build/x.swift 會中）。`.build/` 與 `__pycache__/` 刻意不加，
# 它們本來就該在任何深度都被忽略。
/build/
# tools/ 的 Python bytecode 快取。跑一次 test_pipeline.py 就會冒出來。
__pycache__/

# M6 素材工作區：原圖與中間產物不進版控。
#
# 前導斜線是必要的，不是風格。沒有它，`packs/` 會匹配**任何深度**的 packs 目錄，
# 而 macOS 的 core.ignorecase=true 讓它連大小寫都不分——於是內建 pack 的家
# `Sources/FindMouseAdapters/Resources/Packs/` 一起被吃掉。已經 tracked 的檔案
# 不受 ignore 影響，所以現況看不出問題；下次重建 mycat 多出一張幀時才會發作：
# 新的 PNG 被靜默排除，pack.json 宣告的格數比實際檔案多，而 git status 一個字都不說。
/raw/
/work/
/packs/

# superpowers 的規格與實作計畫。刻意不進版控（2026-08-11 連同歷史一起清除）。
# 它們仍是設計權威，CLAUDE.md 與 README 都指著它們——備份由使用者自行安排，
# repo 這一側不再持有任何副本。
/docs/superpowers/
